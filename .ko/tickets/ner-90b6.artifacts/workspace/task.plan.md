nerve dev shell
  just dev      — run Tauri (hot-reload)
  just dev-ui   — run frontend only (fake backend)
  just test     — run full test suite
  just build    — build release binary
  just          — list all recipes
Perfect. The existing plan is accurate and matches the current state of the code. The implementation has not been done yet. The plan is comprehensive and ready to be executed. No updates needed.

The plan at `/home/dev/Projects/nerve/.ko/tickets/ner-90b6.artifacts/plan.md` is complete, accurate, and ready for implementation. It correctly identifies:

1. ✅ The goal: distinguish mention badges from general unreads
2. ✅ The context: files involved, data model, testing patterns, Nix environment requirement
3. ✅ The approach: update badge conditions, rename function, add tests, update spec
4. ✅ 6 ordered tasks with specific file locations and verification steps
5. ✅ No open questions (the data model already supports this distinction)

The plan is ready. No changes needed.
