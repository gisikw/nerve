nerve dev shell
  just dev      — run Tauri (hot-reload)
  just dev-ui   — run frontend only (fake backend)
  just test     — run full test suite
  just build    — build release binary
  just          — list all recipes
The implementation is complete and correct. All three plan tasks were executed as specified. The one gap — no spec update for the new icon behavior — was caught and fixed by adding a "Logout button displays icon" scenario to `specs/ui_layout.feature`. All 387 tests pass.

```json
{"disposition": "continue"}
```
