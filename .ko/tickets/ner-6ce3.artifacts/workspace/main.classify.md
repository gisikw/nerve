nerve dev shell
  just dev      — run Tauri (hot-reload)
  just dev-ui   — run frontend only (fake backend)
  just test     — run full test suite
  just build    — build release binary
  just          — list all recipes
Looking at the ticket: "Can we replace the logout button text with an icon?"

This is a straightforward **feature request** asking for a specific code change—replacing the logout button's text with an icon. The ticket describes what to build, not what's broken or what needs investigation.

```json
{"disposition": "route", "workflow": "task"}
```
