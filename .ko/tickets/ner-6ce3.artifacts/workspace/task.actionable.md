nerve dev shell
  just dev      — run Tauri (hot-reload)
  just dev-ui   — run frontend only (fake backend)
  just test     — run full test suite
  just build    — build release binary
  just          — list all recipes
```json
{"disposition": "continue"}
```

The plan is clear, complete, and actionable:

- **No open questions** — explicitly addressed; the icon pattern is established from `#streams-toggle`
- **Well-defined scope** — 3 ordered tasks with specific file paths and line numbers
- **Clear verification steps** — each task includes what to check (visual rendering, test passage, CSS assertions)
- **Informed by context** — references existing patterns in the codebase and provides specific CSS properties to modify

The plan is ready for implementation.
