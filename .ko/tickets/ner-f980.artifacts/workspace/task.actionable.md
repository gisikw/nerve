nerve dev shell
  just dev      — run Tauri (hot-reload)
  just dev-ui   — run frontend only (fake backend)
  just test     — run full test suite
  just build    — build release binary
  just          — list all recipes
```json
{"disposition": "continue"}
```

The plan is complete and actionable:

✓ **Clear goal** — Bold channels with unreads in the switcher  
✓ **No open questions** — Explicitly states none; follows established sidebar pattern  
✓ **Identified files** — Lists specific files with line numbers for all changes  
✓ **Ordered tasks** — 3 sequential tasks with verification steps for each  
✓ **Feasible** — References existing RoomInfo properties (notification_count, highlight_count) and an established CSS pattern from the sidebar

The plan is ready for implementation.
