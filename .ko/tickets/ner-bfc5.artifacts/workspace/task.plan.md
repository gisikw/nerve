nerve dev shell
  just dev      — run Tauri (hot-reload)
  just dev-ui   — run frontend only (fake backend)
  just test     — run full test suite
  just build    — build release binary
  just          — list all recipes
The plan is current and accurate. The CSS file matches the plan's diagnosis exactly — the plan is ready for implementation with no changes needed.
